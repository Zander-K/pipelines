String getWorkflowName(String githubWorkflow) {
  // final String name;
  // var parts = githubWorkflow.split('/');
  // final name = parts.length >= 3 ? parts[2] : 'Unknown Workflow';

  // if (githubWorkflow.toLowerCase().contains('ios')) {
  //   name = 'iOS';
  // } else if (githubWorkflow.toLowerCase().contains('android')) {
  //   name = 'Android';
  // } else {
  //   name = 'Unknown';
  // }
  return githubWorkflow;
}
